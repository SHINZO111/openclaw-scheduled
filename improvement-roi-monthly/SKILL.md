---
name: improvement-roi-monthly
description: 毎月10日09:00 JST - 先月の改善変更がメトリクスを実際に改善したか検証・ROI計測
---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
**[EXEC-DIRECTIVE] このプロンプトにすべての手順が含まれています。外部ファイルの読み取りは不要です。scheduledディレクトリ等の探索はせず、直接タスクを開始してください。**

⚠️ **スケジュール実行の必須ルール**: どのステップで失敗・中断しても、必ず何らかのメッセージを返して終了すること。空のままタスクを終了しないこと（空レスポンスはシステムエラーとして記録される）。

# improvement-roi-monthly
# 実行時刻: 毎月10日 09:00 JST
# 目的: 先月実施した改善変更（soul-evolution-proposals承認済み）が実際にメトリクスを改善したかを検証し、「変更→効果」のパターンを学習する

---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
## Step 1: 先月の承認済み変更一覧の取得

`C:\Users\sawas\.openclaw\workspace\memory\soul-evolution-proposals.md` を読み込む。

先月（前月のYYYY-MM）のセクションから「✅ 承認」マークのついた変更を抽出:
- 提案ID（QR-XX / PTO-XX / GA-XX 等）
- 変更内容の要約
- 変更対象（タスク名 / ファイル名）
- 実施日

---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
## Step 2: 変更前後のメトリクス比較

`C:\Users\sawas\.openclaw\workspace\memory\growth-metrics.md` を読み込む。
`C:\Users\sawas\.openclaw\workspace\memory\x-performance-log.md` を読み込む。

各承認済み変更について、変更実施前後のメトリクスを比較:

| 提案ID | 変更内容 | 変更対象指標 | 変更前値 | 変更後値 | 変化率 | ROI判定 |
|-------|---------|-----------|---------|---------|-------|-------|
| QR-01 | [変更内容] | エンゲージメント率 | [X]% | [Y]% | [+Z%] | ✅ 有効 / ❌ 無効 / ⚠️ データ不足 |

**ROI判定基準**:
- ✅ 有効: 対象指標が+5%以上改善
- → 通常: ±5%以内の変化
- ❌ 無効: 対象指標が-5%以下悪化
- ⚠️ データ不足: 変更後1ヶ月未満または計測データなし

---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
## Step 3: パターン学習の記録

`C:\Users\sawas\.openclaw\workspace\memory\learnings.md` に本日セクション追記:

```
## YYYY-MM-DD (improvement-roi-monthly)

### 有効だった改善
- [提案ID] [変更内容]: [指標] が [変化率] 改善 → パターン: [なぜ効いたかの推定]

### 無効だった改善
- [提案ID] [変更内容]: [指標] が [変化率] 悪化 → 仮説: [なぜ効かなかったか]
- → 次回: [改善の修正方向]

### データ不足
- [提案ID]: 引き続き来月も追跡
```

cronログファイルにも記録:
`C:\Users\sawas\.openclaw\workspace\memory\cron-logs\improvement-roi-monthly.md` に以下を追記:
```
## YYYY-MM ROI検証記録
- [提案ID] [変更内容] → [ROI判定] ([変化率])
```

---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
## Step 4: 無効変更のロールバック提案

❌ 無効と判定された変更があった場合、`soul-evolution-proposals.md` に追記:

```
## 提案 YYYY-MM-ROLLBACK（improvement-roi-monthly 自動生成）

### ロールバック提案

#### 提案ROI-01: [提案ID] のロールバック
- 変更内容: [内容]
- 結果: [指標] が [変化率] 悪化
- 推奨: ロールバックまたは代替案の検討

✅ 承認 → 次回capable-evolver-daily実行時にロールバック実施
```

---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
## Step 5: 改善成功率の集計

`growth-metrics.md` に追記:

```
## 改善ROI集計 YYYY-MM-DD
| 期間 | 有効 | 無効 | データ不足 | 改善成功率 |
|-----|-----|-----|---------|---------|
| YYYY-MM | [N] | [M] | [K] | [N/(N+M)]% |
```

過去3ヶ月の成功率推移も記録:
- 改善成功率が50%を下回る場合: 提案生成プロセス自体の見直しをalerts.mdに警告

---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
## Step 6: alerts.md 通知

```
| YYYY-MM-DD 09:00 | 🔵 INFO | 改善ROI検証完了 | 有効[N]件 / 無効[M]件 / 成功率[X]% | 解決済み |
```

無効変更がある場合:
```
| YYYY-MM-DD 09:00 | 🟡 MEDIUM | 改善ロールバック提案 | [N]件の改善が逆効果 → soul-evolution-proposals.md 確認 | 未解決 |
```

---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
## 完了条件
- [ ] 先月承認済み変更一覧取得済み
- [ ] 変更前後メトリクス比較完了
- [ ] learnings.md / cronログファイル にパターン学習記録済み
- [ ] 無効変更のロールバック提案生成済み（対象なしの場合はスキップ）
- [ ] growth-metrics.md 改善成功率追記済み
- [ ] alerts.md 通知済み

---
name: content-recycler-weekly
description: 毎週日曜22:00 JST - X投稿TOP3をNote.com記事・スレッド候補に自動転換キューイング
---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
**[EXEC-DIRECTIVE] このプロンプトにすべての手順が含まれています。外部ファイルの読み取りは不要です。scheduledディレクトリ等の探索はせず、直接タスクを開始してください。**

⚠️ **スケジュール実行の必須ルール**: どのステップで失敗・中断しても、必ず何らかのメッセージを返して終了すること。空のままタスクを終了しないこと（空レスポンスはシステムエラーとして記録される）。

# content-recycler-weekly
# 実行時刻: 毎週日曜22:00 JST
# 目的: 今週のX投稿TOP3を特定し、より深い価値を提供できるNote.com記事候補・Xスレッド候補として自動キューイングする

---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
## Step 1: 今週のTOP3特定

`C:\Users\sawas\.openclaw\workspace\memory\x-performance-log.md` を読み込む。

今週（YYYY-WXX）のエンゲージメントデータから TOP3 トピックを特定する。

データがない場合は、weekly-trends.md の「今週の注目トレンド」上位3件を代替として使用する。

---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
## Step 2: リサイクル価値の判定

TOP3の各トピックについて以下を評価:

**Note.com記事化スコア（0-3点）**:
- 深掘りできる技術的内容がある → +1
- 一般読者にも価値がある → +1
- 1000文字以上の内容を書ける → +1

**Xスレッド化スコア（0-3点）**:
- 5つ以上のサブポイントに分解できる → +1
- ストーリー構造がある → +1
- 数字・データを含む → +1

---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
## Step 3: コンテンツキューへの追加

`C:\Users\sawas\.openclaw\workspace\memory\content-queue.md` を読み込む（なければ作成）。

ファイル新規作成時のテンプレート:
```markdown
# コンテンツキュー

> content-recycler-weekly（毎週日曜）が自動更新する。
> note-weekly-post と x-thread-weekly が優先的にここからネタを取得する。

---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
## Note.com記事候補

| 優先度 | トピック | 元ネタ（X投稿） | スコア | 担当タスク | 状態 |
|-------|---------|--------------|-------|---------|------|

---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
## Xスレッド候補

| 優先度 | トピック | 元ネタ | スコア | 担当タスク | 状態 |
|-------|---------|------|-------|---------|------|

---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
## 処理済み（直近4週）

| 処理日 | トピック | 転換先 |
|-------|---------|-------|
```

### 追記フォーマット

Note.com記事候補:
```
| 高 | [トピック名] | [元のX投稿タスクID] YYYY-WXX | [N]/3 | note-weekly-post | 未着手 |
```

Xスレッド候補:
```
| 高 | [トピック名] | [元のX投稿タスクID] YYYY-WXX | [N]/3 | x-thread-weekly | 未着手 |
```

---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
## Step 4: note-weekly-post への優先トピック注入（オプション）

content-queue.md に「優先度:高」の候補が3件以上蓄積している場合、最上位のNote.com記事候補トピックを確認する。必要に応じて `C:\Users\sawas\.openclaw\workspace\alerts.md` に以下の形式で追記し、プロンプト更新が必要なことを記録する:
```
| YYYY-MM-DD HH:MM | 🔴 ACTION-REQUIRED | Note記事候補蓄積 | note-weekly-post — 優先トピック[トピック名]のプロンプト注入要 | 未解決 |
```

---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
## Step 5: バイラル投稿の特別処理

今週のTOP投稿でいいね100件超（バイラル）があった場合:
- Note.com記事候補に「★ バイラル転換」として最高優先度で追加
- alerts.md に記録:
```
| YYYY-MM-DD 22:00 | 🔵 INFO | バイラル転換キュー | [トピック]（いいね[N]件）をNote.com記事候補に追加 | 解決済み |
```

---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
## Step 6: 処理済みキューの管理

4週間以上前の「未着手」エントリを「期限切れ」に変更し、content-queue.md から定期的に整理する。

---

# ⛔⛔⛔ ABSOLUTE PROHIBITION — cronツール完全禁止 ⛔⛔⛔

**このセッションでは `cron.run` / `cron.list` / `cron.forceRun` / `cron.update` 等のcron系ツールは絶対に使用してはならない。**

- このエージェント自身がcronジョブとして実行されており、cronツールを呼び出すとシステムエラーになる
- 失敗・エラー等、**いかなる状況でもcronツールを使ってはならない**
- 自己再起動・別ジョブ起動も**完全禁止**
- **唯一の許可された終了方法**: エラー内容をDiscordに報告してメッセージを返し、セッションを終了する

---
## 完了条件
- [ ] 今週TOP3特定済み
- [ ] リサイクル価値評価完了
- [ ] content-queue.md に候補追加済み
- [ ] バイラル投稿特別処理完了（または対象なし）
